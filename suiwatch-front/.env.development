# API base URL - leave empty to use Vite proxy (local dev), or set to production URL
# For local development with wrangler dev running on port 8787:
VITE_API_BASE=http://localhost:8787
# For production:
# VITE_API_BASE=https://suiport.mailberkayoztunc.workers.dev
